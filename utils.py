def square_number(number: int) -> int:
    return number


def repeat_text(text: str, times: int) -> str:
    text_many_times_repeated = text * times
    return text_many_times_repeated


def get_last_element(items: list) -> any:
    last_element = items[-1]
    return last_element
